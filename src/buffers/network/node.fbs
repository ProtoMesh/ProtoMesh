include "uuid.fbs";
include "crypto.fbs";

namespace lumos.network;

table Endpoint {
    // Nothing here
}

table Node {
    uuid: UUID (required);
    publicKey: crypto.PublicKey (required);
    endpoints: [Endpoint];
}

file_identifier "NODE";
root_type Node;