namespace protoMesh.crypto;

table PublicKey {
    compressed: [ubyte] (required);
}

table Signature {
    data: [ubyte] (required);
    used_key: [ubyte] (required); // TODO Replace with PublicKey.hash
}