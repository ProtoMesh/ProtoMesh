include "uuid.fbs";

namespace lumos.registry.sync;

enum RequestType: byte {
    HASH,
    ENTRIES
}

table Request {
    type: RequestType;
    target: UUID (required);
    requestID: UUID;
    index: ulong;
}

file_identifier "RREQ";
root_type Request;