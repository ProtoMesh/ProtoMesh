include "../entry.fbs";

namespace lumos.registry.sync;

table Entries {
    name: string (required);
    instance: UUID (required);

    entries: [lumos.registry.Entry];
}

file_identifier "RSEN";
root_type Entries;