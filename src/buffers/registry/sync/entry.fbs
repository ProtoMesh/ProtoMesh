include "../entry.fbs";

namespace lumos.registry.sync;

table Entry {
    name: string (required);
    instance: UUID (required);

    entry: lumos.registry.Entry;
}

file_identifier "RSEN";
root_type Entry;