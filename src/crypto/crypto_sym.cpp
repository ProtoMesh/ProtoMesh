#include "crypto.hpp"

namespace Crypto {
    namespace sym {
        vector<uint8_t> encrypt(string text, string key) {
//            vector<uint8_t> in(text.begin(), text.end());
//            HASH hashedKey(Crypto::hash::sha512Vec(key));
//            vector<uint8_t> out(text.size());

            // TODO Encrypt to out

//            return out;
            return {};
        }
    }
}