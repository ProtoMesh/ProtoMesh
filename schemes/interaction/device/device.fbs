include "./interface/endpoint.fbs";

namespace protoMesh.interaction.device;

table Device { // TODO encrypt with shared secret of querying user
	endpoints: [interface.Endpoint];
}

file_identifier "DEVI";
root_type Device;