include "./interface/endpoint.fbs";

namespace ProtoMesh.scheme.interaction.device;

table Device {
    // ***
    // * List of endpoints
    // * Reduced to querying users permitted ones.
    // ***
	interface: [interface.Endpoint];
}

file_identifier "DEVI";
root_type Device;